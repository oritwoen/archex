pkgname=vte

arch=(x86_64)
pkgver=0.79.0+5607+c90b078e
pkgrel=1

b2sums=(SKIP)
source=(git+https://gitlab.gnome.org/gnome/vte.git)
#options=(!lto)

provides=(vte3 vte4 vte-common)
replaces=(vte3 vte4 vte-common)
conflicts=(vte3 vte4 vte-common)

depends=(
	gtk4
	cairo
	pango
	systemd
)

makedepends=(
	git
	vala
	meson
	gobject-introspection
)

pkgver() {
	cd $pkgname

	local _version=$(grep -m 1 version: meson.build | awk -F"'" '{print $2}')
	local _commits=$(git rev-list --count HEAD)
	local _commith=$(git rev-parse --short HEAD)

	echo ${_version}+${_commits}+${_commith}
}

build() {
	local options=(
		-D a11y=true #TODO: verify
		-D gir=true #TODO: verify
		-D gtk3=false
	)

	arch-meson $pkgname build "${options[@]}"

	meson compile -C build
}

package() {
	meson install -C build --destdir "$pkgdir"
}
